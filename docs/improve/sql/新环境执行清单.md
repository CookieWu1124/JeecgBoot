# 新环境数据库脚本执行清单

> 前提：**Jeecg 基础库已导入**（本清单不再包含基础库）。  
> 目标：MySQL 5.7+（`utf8mb4`）。  
> 按序号 **从上到下依次执行**，不要跳步、不要跑 `fix/`。

---

## 一、必跑（3 个）

| 序号 | 文件 | 作用 |
|------|------|------|
| 1 | `docs/improve/sql/proposal_init.sql` | 提案业务表 + 角色 + 改善性质/评分维种子 |
| 2 | `docs/improve/sql/proposal_menu.sql` | 提案改善菜单/按钮 + admin、proposal_admin 授权 |
| 3 | `docs/improve/sql/sms_menu.sql` | 销售管理菜单/按钮 + admin 授权 |

```bash
mysql -u root -p <新库名> < docs/improve/sql/proposal_init.sql
mysql -u root -p <新库名> < docs/improve/sql/proposal_menu.sql
mysql -u root -p <新库名> < docs/improve/sql/sms_menu.sql
```

执行后：管理端 **退出重新登录**，确认「提案改善」「销售管理」菜单可见。

---

## 二、联调可选（需要 HR 人员/配置名册时再跑）

| 序号 | 文件 | 作用 |
|------|------|------|
| 4 | `docs/improve/sql/hr_user_seed_all.sql` | HR 组织 + 用户全量种子 |
| 5 | `docs/improve/sql/proposal_config_seed.sql` | 部门负责人 / 委员会 / 批准人（依赖第 4 步） |

```bash
mysql -u root -p <新库名> < docs/improve/sql/hr_user_seed_all.sql
mysql -u root -p <新库名> < docs/improve/sql/proposal_config_seed.sql
```

---

## 三、本次不要执行

| 文件/目录 | 原因 |
|-----------|------|
| Jeecg 基础库 SQL | 已执行过 |
| `docs/improve/sql/hr_user_purge.sql` | 新库无旧数据可清；仅「重导 HR」时用 |
| `docs/improve/sql/fix/**` 全部 | 旧库增量；新环境内容已并入上面全量脚本 |

---

## 勾选栏

- [ ] 1 `proposal_init.sql`
- [ ] 2 `proposal_menu.sql`
- [ ] 3 `sms_menu.sql`
- [ ] （可选）4 `hr_user_seed_all.sql`
- [ ] （可选）5 `proposal_config_seed.sql`
- [ ] 退出重新登录，菜单核对通过
