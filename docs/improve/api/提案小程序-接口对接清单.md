# 提案改善 · 小程序接口对接清单（申请段）

> **发给谁**：负责 spex-app / 小程序提案业务的前端同事  
> **覆盖范围**：申请段已落地接口（发起 → 委员审核 → 批准人决策）  
> **不在本清单**：任务分配、计划书、报告书、评分签核（后续分段再补）  
> **版本日期**：2026-08-31  
> **联调样例单**：`202608290001`（贺志龙提交；委员 5/5 后进入待核定）

关联文档：

| 文档 | 用途 |
|------|------|
| [移动端本地联调指南.md](../移动端本地联调指南.md) | 环境启动、H5/Apifox 联调步骤 |
| [progress/01_application_DEV_PROGRESS.md](../progress/01_application_DEV_PROGRESS.md) | 申请段进度与验收 |
| 代码参考 | `spex-app/src/api/proposal.ts`（本仓库已对接实现） |

---

## 1. 联调约定（先看这段）

| 项 | 说明 |
|----|------|
| Base URL | `http://{host}:8080/jeecg-boot` |
| 鉴权 | 请求头 `X-Access-Token: <JWT>`（与管理端同一套） |
| 登录 | `POST /sys/mLogin`，`username` = **工号**（`workNo`） |
| 密码 | **禁止明文**：AES-CBC + PKCS7，Key/IV 见下文 A1；示例代码见 `spex-app/src/utils/cipher.ts` |
| Content-Type | JSON 接口：`application/json` |
| 上传 | `multipart/form-data` → `/sys/common/upload` |
| 统一响应 | Jeecg `Result`：`{ success, code, message, result }`；业务成功常见 `code` 为 `200` 或 `0` |
| 业务报错 | `success=false`，`message` 为中文提示，前端直接 toast 即可 |
| Knife4j | `http://localhost:8080/jeecg-boot/doc.html` → 标签「提案业务」 |

### 1.1 路径前缀约定

| 前缀 | 用途 | 本清单 |
|------|------|--------|
| `/proposal/**` | **共用**（小程序主链路） | ✅ 重点 |
| `/proposal/meta/**` | 元数据 | ✅ |
| `/sys/**` | 登录 / 上传 | ✅ 必要 |
| `/proposal/admin/**` | 管理端 | ❌ 不对接 |
| `/proposal/app/**` | 小程序首页聚合（可选） | ⚪ 可选 |

### 1.2 申请段状态流转（前端展示用）

```text
POST /proposal/create ──► PENDING_REVIEW（审核中）── review_progress: 0/N → N/N
              └─ 委员全部提交 ──► PENDING_APPROVAL（待批准）
                                    ├─ 批准 ──► APPROVED（已批准）← 申请段结束；阶段2待定，不是待指派
                                    └─ 不批准 ──► REJECTED_FINAL（不批准）
```

申请段**没有暂存、没有撤回**。`PENDING_ASSIGN` 不进 `GET /proposal/meta/statuses` 默认列表。无 `DRAFT` / `WITHDRAWN`。

发起页两步只是前端确认，点提交才调一次 `POST /proposal/create`，直接进入审核中。

| 状态码 | 中文 | 谁可操作 |
|--------|------|----------|
| `PENDING_REVIEW` | 审核中 | 委员：审 |
| `PENDING_APPROVAL` | 待批准 | 批准人：决策 |
| `APPROVED` | 已批准 | 申请段通过（阶段 2 待定） |
| `REJECTED_FINAL` | 不批准 | 终态 |

改善性质（多选，存 JSON 数组字符串）：码来自配置表 `proposal_improvement_type`，**不要写死**。发起页芯片调 `GET /proposal/meta/improvementTypes`（默认仅启用）。列表/详情优先用行内 `improvementTypesLabel`。种子码仍为：

| value | 文案（种子，可在管理端改名/停用/新增） |
|-------|------|
| `SAFETY` | 安全改善 |
| `QUALITY` | 品质改善 |
| `EFFICIENCY` | 效率改善 |
| `DELIVERY` | 交付改善 |
| `COST` | 成本改善 |

示例：`improvementTypes = "[\"SAFETY\",\"EFFICIENCY\"]"`

---

## 2. 接口总表（一页看完）

| # | 场景 | 方法 | 路径 | 角色 | spex-app 方法 |
|---|------|------|------|------|---------------|
| A1 | 登录 | POST | `/sys/mLogin` | 全员 | 登录页 |
| A2 | 上传图片 | POST | `/sys/common/upload` | 提案人 | `uploadProposalImage` |
| B1 | 改善部门列表 | GET | `/proposal/meta/improvementDepts` | 提案人 | `fetchImprovementDepts` |
| B2 | 提案状态选项 | GET | `/proposal/meta/statuses` | 全员 | `fetchProposalStatuses` |
| B3 | 改善性质选项 | GET | `/proposal/meta/improvementTypes` | 全员 | `fetchImprovementTypes` |
| C1 | 发起提案 | POST | `/proposal/create` | 提案人 | `createProposal` |
| C3 | 单独提交（已并入 C1） | PUT | `/proposal/{id}/submit` | — | 勿对接；重复调用若已是审核中则幂等成功 |
| C5 | 我的列表 | GET | `/proposal/list` | 提案人 | `fetchProposalList` |
| C6 | 详情 | GET | `/proposal/{id}` | 提案人/委员/批准人* | `fetchProposalDetail` |
| D1 | 委员待审列表 | GET | `/proposal/review/committee/pending` | 委员 | `fetchCommitteePending` |
| D2 | 委员提交意见 | POST | `/proposal/review/committee/{proposalId}` | 委员 | `submitCommitteeReview` |
| E1 | 批准人待办 | GET | `/proposal/approval/pending` | 批准人 | `fetchApprovalPending` |
| E2 | 申请批准决策 | POST | `/proposal/approval/application/{proposalId}` | 批准人 | `submitApplicationApproval` |

\* 详情权限：提案人始终可看自己的；委员在待审名册内可看；批准人在在任时可看待核定/已决策相关单。非授权会业务报错。

---

## 3. 鉴权与基础设施

### A1 登录

```http
POST /sys/mLogin
Content-Type: application/json
```

```json
{
  "username": "600081",
  "password": "<AES-CBC 加密后的 Base64 密文，不要传明文>"
}
```

| 字段 | 说明 |
|------|------|
| `username` | 工号（`workNo`） |
| `password` | **必须先 AES-CBC 加密再传**；明文会被后端鉴权失败（或按环境回退策略不稳定，联调请一律加密） |
| `loginOrgCode` | 可选；多部门时传所选 `orgCode` |

成功后取 `result.token`，后续请求带：

```http
X-Access-Token: <token>
```

#### ⚠️ 密码加密（必读）

与管理端 `jeecgboot-vue3`、后端 `org.jeecg.common.util.encryption.EncryptedString` **同一套参数**：

| 项 | 值 |
|----|-----|
| 算法 | AES |
| 模式 | CBC |
| 填充 | PKCS7 |
| Key（16 字节） | `1234567890adbcde` |
| IV（16 字节） | `1234567890hjlkew` |
| 输出 | CryptoJS 默认 **Base64** 字符串（即 `.toString()`） |

> 依赖：`crypto-js`。Key/IV 改了必须与后端同步，否则登录必失败。  
> 同源参考：`spex-app/src/utils/cipher.ts`、`spex-app/src/api/login.ts`  
> 同样需要加密的接口：`POST /sys/loginGetUserDeparts`（取部门列表）的 `password` 字段。

**加密工具示例（可直接拷贝）：**

```ts
// spex-app/src/utils/cipher.ts
import CryptoJS from 'crypto-js'

/** 与后端 EncryptedString.key 一致，长度 16 */
export const AES_KEY = '1234567890adbcde'
/** 与后端 EncryptedString.iv 一致，长度 16 */
export const AES_IV = '1234567890hjlkew'

/**
 * AES CBC + PKCS7 加密，输出 CryptoJS 默认 Base64 密文
 * 后端 AesEncryptUtil.resolvePassword 可解密
 */
export function encryptAESCBC(plainText: string): string {
  if (!plainText)
    return plainText
  const key = CryptoJS.enc.Utf8.parse(AES_KEY)
  const iv = CryptoJS.enc.Utf8.parse(AES_IV)
  return CryptoJS.AES.encrypt(plainText, key, {
    iv,
    mode: CryptoJS.mode.CBC,
    padding: CryptoJS.pad.Pkcs7,
  }).toString()
}
```

**调用示例：**

```ts
// spex-app/src/api/login.ts（节选）
import { encryptAESCBC } from '@/utils/cipher'

export function login(loginForm: { username: string, password: string, orgCode?: string }) {
  return http.post('/sys/mLogin', {
    username: loginForm.username,
    password: encryptAESCBC(loginForm.password), // ← 明文先进加密，再作为 body.password
    loginOrgCode: loginForm.orgCode,
  })
}
```

### A2 现场图片上传

```http
POST /sys/common/upload
Content-Type: multipart/form-data
X-Access-Token: <token>
```

- 表单字段名一般为 `file`
- 返回 `result` 为文件访问路径/URL（按 Jeecg 实际上传结果解析）
- 业务约定：**最多 4 张**；发起时把 `fileName` / `fileUrl` / `fileSize` 写入 `attachments`

---

## 4. 元数据

### B1 改善部门下拉

```http
GET /proposal/meta/improvementDepts
```

**`result` 数组元素：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `deptId` | string | 改善部门 ID（提交时用） |
| `deptName` | string | 部门名称 |
| `leaderUserId` | string | 负责人用户 ID（可空） |
| `leaderName` | string | 负责人姓名（可空） |
| `leaderConfigured` | boolean | 是否已配置负责人；未配置时提交会被拒 |

### B2 提案状态下拉（枚举，非字典）

```http
GET /proposal/meta/statuses
```

文案唯一来源：后端 `ProposalStatusEnum`。列表/详情行上同时带 `statusLabel`，展示优先用行内字段；本接口给筛选下拉。

**`result` 数组元素：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | string | 落库状态码，如 `PENDING_REVIEW` |
| `label` | string | 中文，如 `审核中` |
| `terminal` | boolean | 申请段闭环：已批准 / 不批准 为 true |

默认只返回申请段四档。需要全量枚举时加 `?applicationOnly=false`。

### B3 改善性质芯片/下拉（配置表，非字典）

```http
GET /proposal/meta/improvementTypes?enabledOnly=true
```

文案来源：`proposal_improvement_type`。默认只返回启用项（发起芯片、管理端筛选）。`enabledOnly=false` 含停用行，用于旧单回显。列表/详情行上同时带 `improvementTypesLabel`。

**`result` 数组元素：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | string | 落库码，如 `SAFETY` |
| `label` | string | 中文，如 `安全改善` |
| `typeStatus` | string | `active` / `disabled` |
| `sortNo` | number | 排序 |
| `description` | string | 说明，可空 |

---

## 5. 提案人：发起 / 列表 / 详情

### C1 发起提案（一次提交）

```http
POST /proposal/create
```

**Body：**

```json
{
  "title": "车间灯光太阳能改善测试001",
  "improvementTypes": "[\"SAFETY\",\"EFFICIENCY\"]",
  "deptId": "spexdeptgroupelc0000000000001",
  "teamType": "PERSONAL",
  "currentSituation": "目前状况……",
  "improvementSuggestion": "改善意见……",
  "attachments": [
    {
      "fileName": "现场1.jpg",
      "fileUrl": "/sys/common/static/xxx.jpg",
      "fileSize": 123456
    }
  ]
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | 是 | 提案名称 |
| `improvementTypes` | 是 | JSON 数组**字符串**（码须为当前启用的改善性质；来自 B3） |
| `deptId` | 是 | 来自 B1 |
| `currentSituation` | 是 | 目前状况 |
| `improvementSuggestion` | 是 | 改善意见 |
| `teamType` | 否 | 如 `PERSONAL` |
| `attachments` | 否 | 0～4 |

**`result`：** 提案主键 `id`（string）。成功后状态即为 `PENDING_REVIEW`（审核中），生成 `proposalNo`，`reviewProgress` 为 `0/N`，并写入委员审核快照。

失败常见原因：未配部门负责人、委员会为空、必填正文缺失。

### C3 单独提交（已并入 C1）

```http
PUT /proposal/{id}/submit
```

不要再先 create 再 submit。若在 C1 之后再调本接口，且状态已是审核中，会幂等成功；其它状态会失败。

### C5 我的提案列表

```http
GET /proposal/list?tab=mine&title=&pageNo=1&pageSize=20
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `tab` | `mine` | `mine`/`all` 全部；`doing` 审核中/待批准；`done` 已结束（已批准/不批准/已完成等） |
| `title` | — | 模糊搜标题 |
| `pageNo` / `pageSize` | 1 / 10 | 分页 |

**`result`：** MyBatis-Plus 分页结构：

```json
{
  "records": [ { "id", "proposalNo", "title", "status", "statusLabel", "reviewProgress", "adoptCount", "rejectCount", "planRequiredSuggest", "awardSuggestAmount", "dept", "proposer", "..." } ],
  "total": 1,
  "current": 1,
  "size": 20
}
```

列表仅返回**当前登录人为提案人**的数据。委员汇总 / 人员部门嵌套字段同 [E1](#e1-批准人待办)。

### C6 提案详情

```http
GET /proposal/{id}
```

**`result`：**

```json
{
  "proposal": { "id", "proposalNo", "title", "status", "statusLabel", "reviewProgress", "planRequired", "awardAmount", "..." },
  "application": { "currentSituation", "improvementSuggestion", "submitTime" },
  "attachments": [ { "fileName", "fileUrl", "fileSize", "sortNo" } ],
  "committeeReviews": [
    {
      "reviewerId": "...",
      "conclusion": "ADOPT",
      "planRequired": 1,
      "awardSuggestion": 150,
      "comment": null,
      "reviewTime": "2026-08-29 15:00:00"
    }
  ],
  "applicationApproval": {
    "approverId": "...",
    "stage": "APPLICATION",
    "decision": "APPROVE",
    "planRequired": 1,
    "awardAmount": 150,
    "comment": null,
    "approveTime": "2026-08-29 16:00:00"
  }
}
```

| 字段 | 说明 |
|------|------|
| `committeeReviews` | 含**未审**行：`conclusion` / `reviewTime` 为空 |
| `applicationApproval` | 未做决策时可能为 `null` |
| `statusLogs` | 管理端详情用；小程序可忽略 |

---

## 6. 委员审核

> 身份：用户在 `proposal_committee_member` 在任名册中。  
> 每人一票，**提交后不可改**；全部提交后自动进入 `PENDING_APPROVAL`。

### D1 委员待审列表

```http
GET /proposal/review/committee/pending?pageNo=1&pageSize=20
```

返回：状态为 `PENDING_REVIEW` 且**当前用户尚未提交意见**的提案分页。

每条带委员汇总（未审完也可能是部分票）和人员部门嵌套。**字段名与 JSON 样例见 [E1](#e1-批准人待办)**，D1 / E1 / C5 同一套。

非委员调用：业务提示无权限 / 不在名册。

### D2 委员提交意见

```http
POST /proposal/review/committee/{proposalId}
```

**采用：**

```json
{
  "conclusion": "ADOPT",
  "planRequired": 1,
  "awardSuggestion": 150,
  "comment": "可选说明"
}
```

**不采用：**

```json
{
  "conclusion": "REJECT",
  "comment": "不采用原因（必填）"
}
```

| 字段 | 采用 | 不采用 |
|------|------|--------|
| `conclusion` | `ADOPT` | `REJECT` |
| `planRequired` | **必填** `0`/`1` | 忽略 |
| `awardSuggestion` | 可选 number | 忽略 |
| `comment` | 可选 | **必填** |

成功后该提案 `reviewProgress` 递增；最后一人提交触发 → `PENDING_APPROVAL`。

---

## 7. 批准人申请决策

> 身份：`proposal_approver` 在任。通常**全局一人**。  
> 非批准人调 E1/E2 会失败；待办页应对 E1 **单独 catch**，避免整页挂掉。

### E1 批准人待办

```http
GET /proposal/approval/pending?pageNo=1&pageSize=20
```

返回所有 `PENDING_APPROVAL` 提案分页。待办卡片用的委员汇总、人员部门嵌套一并带回，无需再调详情。

**画面文案 ↔ JSON 字段名（抓报文对照）：**

| 画面 | JSON 路径 | 说明 |
|------|-----------|------|
| 采用 4 | `adoptCount` | 委员 `conclusion=ADOPT` 人数 |
| 不采用 1 | `rejectCount` | 委员 `conclusion=REJECT` 人数 |
| 委员会 5/5 | `reviewProgress` | 主表已有，字符串 `"5/5"` |
| 建议形成计划书 | `planRequiredSuggest` | `1` 形成 / `0` 不形成；尚无采用票则为 `null` |
| 奖励 ¥200 | `awardSuggestAmount` | 采用票奖励建议众数；批准前主表 `awardAmount` 仍为 `null` |
| 品质部 | `dept.departName` | 改善部门嵌套 |
| 刘洋 | `proposer.realname` | 提案人嵌套 |

口径：只统计已提交结论；`planRequiredSuggest` / `awardSuggestAmount` 只看 **ADOPT** 票。不要和批准人核定后的 `planRequired` / `awardAmount` 混用。

**`result.records[]` 样例（抓包可对这一段）：**

```json
{
  "records": [
    {
      "id": "...",
      "proposalNo": "202608230008",
      "title": "车间物料标识标准化",
      "status": "PENDING_APPROVAL",
      "statusLabel": "待批准",
      "reviewProgress": "5/5",
      "adoptCount": 4,
      "rejectCount": 1,
      "planRequiredSuggest": 1,
      "awardSuggestAmount": 200,
      "planRequired": null,
      "awardAmount": null,
      "dept": { "id": "...", "departName": "品质部", "orgCode": "A01A02" },
      "proposer": { "id": "...", "realname": "刘洋", "workNo": "6000xx" }
    }
  ],
  "total": 1,
  "current": 1,
  "size": 20
}
```

### E2 申请批准决策

```http
POST /proposal/approval/application/{proposalId}
```

**批准：**

```json
{
  "decision": "APPROVE",
  "planRequired": 1,
  "awardAmount": 150,
  "comment": "可选"
}
```

→ 状态 `APPROVED`（已批准，申请段结束）；主表写入 `plan_required`、`award_amount`。**不会**变成 `PENDING_ASSIGN`。

**不批准：**

```json
{
  "decision": "REJECT",
  "comment": "不批准原因（必填）"
}
```

→ 状态 `REJECTED_FINAL`。

| 字段 | 批准 | 不批准 |
|------|------|--------|
| `decision` | `APPROVE` | `REJECT` |
| `planRequired` | **必填** `0`/`1` | 忽略 |
| `awardAmount` | 建议填写 | 忽略 |
| `comment` | 可选 | **必填** |

同一提案申请阶段决策**只能一次**。

---

## 8. 页面 ↔ 接口映射（对接对照）

| 小程序页面（spex-app） | 主要接口 |
|------------------------|----------|
| 登录 | A1 |
| 发起提案 `pages/proposal/apply` | B1 → A2 → **C1 一次提交** |
| 提案列表 `pages/proposal/index` | C5 |
| 提案详情 `pages/proposal/detail` | C6 |
| 待办 `pages/todo` | D1 + E1（E1 失败不影响委员待办） |
| 委员审核 `pages/proposal/review` | C6 + D2 |
| 批准核定 `pages/proposal/approve` | C6（看委员汇总）+ E2 |

建议待办跳转带 **提案 `id`**（不要只带编号），审核/批准页用 path 参数调 C6/D2/E2。

---

## 9. 联调账号（inside_dev 实配）

| 角色 | 建议工号 | 姓名 | 用途 |
|------|----------|------|------|
| 提案人 | `600081` | 贺志龙 | 发起/列表（非委员） |
| 委员 | `600013` / `600026` / `600051` / `600088` / `600099` | 曾金 / 陈泽波 / 刘恋 / 张小朋 / 吴浪 | 各审一次 |
| 批准人 | `600084` | 汪秦军 | 待核定决策 |

> `600099`（吴浪）已在委员会：**不要用委员账号当纯提案人**测「自己审自己」边界，除非刻意测互斥。

密码以环境实际为准（种子用户见 `docs/improve/sql/proposal_spex_user_seed.sql`）。

---

## 10. 前端注意点（踩坑清单）

1. **登录密码必须 AES 加密**：`/sys/mLogin`（及 `loginGetUserDeparts`）的 `password` 传密文，算法与 Key/IV 见 §3 A1；明文联调易失败。
2. **`improvementTypes` 是字符串**：`JSON.stringify([...])`，不要直接传数组（除非你们改了后端；当前后端按 string 收）。
3. **委员意见不可二次提交**：按钮提交成功后应禁用/返回待办。
4. **批准人 pending 非全员接口**：普通用户调会报错，待办里 catch 后展示空列表即可。
5. **`reviewProgress` 展示**：字符串如 `"3/5"`，列表/详情直接显示。
6. **`planRequired`**：`0` 不形成计划书，`1` 形成；委员是「建议」，批准人是「核定」并落主表。
7. **Token 失效**：按 Jeecg 约定跳转登录；不要静默吞。
8. **体验登录**：`spex-app` 假 Token **打不到后端**，联调必须用工号真实登录。

---

## 11. 本清单之外（暂未提供）

以下接口**尚未**作为申请段交付，请勿按生产对接：

- 任务指派 / 领取（`PENDING_ASSIGN` 及以后）
- 计划书提交与委员/批准人再审
- 报告书、六维评分、签核结案
- 统计看板真实数据
- `/proposal/app/home` 首页聚合（若未部署可忽略）

有疑问以 Knife4j 实时文档 + 本仓库 `ProposalController` 为准。

---

## 12. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-29 | 初版：申请段（发起/委员并行审核/批准人决策）对接清单归档 |
| 2026-08-29 | A1 登录补充密码 AES-CBC 必读说明，并附 `cipher.ts` / `login.ts` 示例代码 |
| 2026-08-31 | 申请段状态精简为审核中/待批准/已批准/不批准；批准→`APPROVED`；取消撤回 |
| 2026-08-31 | 取消暂存：`POST /proposal/create` 一次进入审核中；C2/C3 不再对接 |
| 2026-08-31 | 删除草稿/撤回接口与 `WITHDRAWN` 状态 |
