---
description: 提案改善 DDL/菜单须与 init、views 路径一致；禁止随意改 sys_permission.component
globs: docs/improve/sql/**,jeecg-boot/**/jeecg-module-spex-inside/**/entity/**,jeecg-boot/**/jeecg-module-spex-inside/**/*.java,jeecgboot-vue3/src/views/mes/proposal/**
alwaysApply: false
---

# 提案改善 — 数据库 DDL 同步规则

## 核心原则

**凡涉及改表结构、字段、索引、约束、种子数据的变更，必须同步更新 `docs/improve/sql/proposal_init.sql`。**

`proposal_init.sql` 是新环境/上线一次性建表的**唯一权威全量脚本**。开发库上的临时 ALTER 若未回写 init，会导致后续环境缺表、缺字段。

目标开发库：**`inside_dev`**（与 `application-dev.yml` 一致）。

## 必须同步的文件

| 顺序 | 文件 | 说明 |
|------|------|------|
| 1 | `docs/improve/sql/proposal_init.sql` | **必改** — 全量 DDL + 幂等种子 |
| 2 | `docs/improve/sql/proposal_tables.md` | 表结构文档与 init 保持一致 |
| 3 | `jeecg-module-spex-inside` 下 `entity/` | Java 字段与表列一致 |
| 4 | 相关 `Mapper` / `Service` | 若字段名或类型变更 |

可选：`docs/improve/sql/fix/` 下增量脚本（**仅用于已有库的迁移**），但 fix 内容最终仍应合并进 `proposal_init.sql` 对应 `CREATE TABLE` 段。

菜单权限以 `docs/improve/sql/proposal_menu.sql` 为准（与业务表 init 分开）。

人员/组织 **快捷查询 SELECT** 以 `docs/improve/sql/hr_quick_queries.md` 为唯一归档（见 `.cursor/rules/improve/proposal-hr-queries.mdc`），勿写入 init/seed/purge。

## 主键 id 生成规则（硬性）

JeecgBoot 默认：`IdType.ASSIGN_ID` → **19 位纯数字雪花 id**（字符串存库，通常 `varchar(32)` / `varchar(36)`）。

| 场景 | 正确做法 | 禁止 |
|------|----------|------|
| 业务代码插入 | 依赖 MyBatis-Plus `ASSIGN_ID`，不要手写 id | 自造 `pr0p0sa*`、`spex*` 等含字母可读串 |
| SQL 种子 / 菜单脚本 | 固定 **19 位纯数字** id（与雪花同形，便于幂等） | `pr0p0sa3001menu…`、带语义字母的 padding id |
| `sys_permission.id` | ≤32 字符的数字雪花 | 超长或字母混排自定义 id |

参考：销售管理菜单 `sms_menu.sql`、联调配置 `proposal_config_seed.sql` 已是数字雪花；提案菜单/角色种子已统一为 `2094101…` / `2094102…` / `2094103…`。

旧库残留 `pr0p0sa*` → 执行 `docs/improve/sql/fix/20260902_fix_pr0p0sa_ids_to_snowflake.sql`。

## 管理端菜单 component 约定（易踩坑）

JeecgBoot 按 `sys_permission.component` 动态加载 `jeecgboot-vue3/src/views/` 下文件。路径不对时页面空白，提示「查看组件引用是否正确」。

| 菜单 | url（权威） | component（权威） | 对应文件 |
|------|-------------|-------------------|----------|
| 提案改善（目录） | `/proposal` | `layouts/RouteView` | 布局，非业务页 |
| 提案管理 | `/proposal/manage` | `mes/proposal/manage/index` | `views/mes/proposal/manage/index.vue` |
| 提案配置 | `/proposal/config` | `mes/proposal/config/index` | `views/mes/proposal/config/index.vue` |

**硬性规则：**

- `component` = 相对 `src/views/` 的路径，**不要**前缀 `views/`，**不要**后缀 `.vue`
- 页面代码在 `views/mes/proposal/`，与 MES 前端同事目录对齐；`component` **必须**带 `mes/` 前缀
- `url` 与 `component` 相互独立：访问路径保持 `/proposal/**`，**不要**写成 `/mes/proposal/**`
- 在「菜单管理」UI 里改提案菜单后，必须核对上表；改完应回写 `proposal_menu.sql`（含下方自愈 UPDATE）
- 仅 `INSERT ... WHERE NOT EXISTS` **不会修复**已存在的错误行；依赖脚本末尾对固定 id 的 `UPDATE` 纠偏

症状速查：空白页「查看组件引用是否正确」或 component 仍是 `proposal/manage/index`（无 `mes/`）→ 执行 `docs/improve/sql/fix/20260831_move_proposal_menu_component.sql` 或重跑 `proposal_menu.sql` 文末 UPDATE。

## 变更检查清单

完成 DDL 相关改动前，确认：

- [ ] `proposal_init.sql` 中对应 `CREATE TABLE` / `INSERT` 已更新
- [ ] 11 项通用字段未遗漏：`create_no`, `create_by`, `create_time`, `update_no`, `update_by`, `update_time`, `sys_org_code`, `sys_org_name`, `tenant_id`, `remark`, `active`
- [ ] 逻辑删除使用 `active`（Y/N），**不要**引入 `del_flag`
- [ ] 唯一索引组合键**末位含 `tenant_id`**
- [ ] 业务状态字段不与 `active` 冲突（如 `member_status`, `approver_status`, `dim_status`）
- [ ] 角色/种子 INSERT 保持幂等（`WHERE NOT EXISTS`）
- [ ] 种子 / 菜单 id 为 **19 位纯数字**（JeecgBoot ASSIGN_ID 风格），无字母自定义 id
- [ ] `proposal_tables.md` 已同步

涉及菜单时额外确认：

- [ ] `proposal_menu.sql` 中 url/component 与上表及 `views/mes/proposal/**` 一致
- [ ] `component` 为 `mes/proposal/...`；`url` 仍为 `/proposal/...`（不要把 `/mes` 写进访问路径）
- [ ] 菜单/按钮 id 为数字雪花，且 ≤32 字符

## init 脚本特性（勿破坏）

- `proposal_*` 业务表段使用 `DROP TABLE IF EXISTS` + `CREATE TABLE` — **重复执行会清空业务数据**
- `sys_role`、六维评分种子 INSERT 为幂等，可重复执行
- Flyway 已关闭，脚本手工执行

## 执行命令参考

```bash
mysql -u root -p inside_dev < docs/improve/sql/proposal_init.sql
mysql -u root -p inside_dev < docs/improve/sql/proposal_menu.sql
```

## 禁止

- 只在 Navicat/开发库手工改表，不回写 `proposal_init.sql`
- 在 entity 新增字段但 init 未建列
- 用 Flyway 片段替代 init 的全量定义（本项目约定手工 init）
- **手写含字母的自定义主键**（如 `pr0p0sa*`）；必须用 JeecgBoot 默认雪花数字 id
- `sys_permission.id` 超过 **32 字符**（JeecgBoot 默认为 `varchar(32)`）
- 在菜单管理或 SQL 中把提案 `component` 改回无 `mes/` 前缀的 `proposal/**`，或把 `url` 改成 `/mes/proposal/**`
